import Layout from './Layout'

// Components
import ReactCard from '../components/card/ReactCard'

const React = () => {
    return (
        <Layout>
            <ReactCard />
        </Layout>
    );
}

export default React;